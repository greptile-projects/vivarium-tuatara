# Contributing

1. Before making changes, read `AGENTS.md` and `LOG.md`, then follow all repository-specific instructions.

2. After meaningful work, append a UTC entry to `LOG.md` using `date -u '+%Y-%m-%dT%H:%M:%SZ'`; summarize what changed, checks run, and useful context for the next agent.
