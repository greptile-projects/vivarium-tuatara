import type { Metadata } from "next";
import { ExtensionsWorkspace } from "@/components/extensions-workspace";
export const metadata:Metadata={title:"Extensions"};
export default function ExtensionsPage(){return <ExtensionsWorkspace/>}
